
on a un menu contolleur
On peut regler notre detection avec les premier regle 
on regle le (lowH,HighH)(lowS,HighS)(LowV,HighV) pour la detection
De base il est regler pour detecter du bleu

On voit se qui est detecter dans la fenetre reglage

les regle bleu vert et rouge permette de regler la couleur du tracer


La regle "dessiner" active le dessin l'orsque on le mait a 1 a 0 rien ne se passe


La regle "Moyenne" permette de faire un seul point au centre de notre detection
Donc d'etre plus précis;

A compiler avec VisualStudio 